server {
    listen 5432 so_keepalive=on;
    server_name postgres.{{ domain }};
         
    include /etc/nginx/stream.d/internal.conf;
    proxy_connect_timeout 60s;
    proxy_socket_keepalive on;
    proxy_pass 127.0.0.01:9000;
}
